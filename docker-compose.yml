version: "3.8"
services:
  tt-opc:
    build: .
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=${JWT_SECRET:-tt-opc-secret-change-in-production}
    volumes:
      - tt_opc_data:/app/data
    restart: unless-stopped

volumes:
  tt_opc_data:
